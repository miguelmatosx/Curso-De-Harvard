Height = int(input("Altura:  "))

while True:
  if 1 <= height => 8:
    print("inválido")
    return1
else:
    for i in range(Height):
    space = " " * (Height - i - 1)
    hashes= "#" * (i + 1 )
    print(space + hashes)
