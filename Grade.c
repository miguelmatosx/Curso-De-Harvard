#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main(void)
{
    string texto = get_string ("Insira o Texto: ");

    int letras
    for( int l = 0 ; l < strlen(texto) ; l++);
    {
        if(isalpha(texto[l]));
        {
            
        }
    }

}
